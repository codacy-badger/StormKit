language: cpp
sudo: false
branches:
  only:
    - dev

env:
  global:
    - TRAVIS_BUILD_TYPE=Debug

matrix:
  include:
    - os: linux
      compiler: clang
      sudo: required
    - os: osx
      compiler: clang

